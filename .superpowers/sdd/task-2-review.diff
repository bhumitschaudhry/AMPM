# Review package: 2144ed5..8b88a8d

## Commits
8b88a8d feat: store Clerk identities on users

## Files changed
 .../prisma/migrations/20260715120000_add_clerk_identity/migration.sql  | 3 +++
 server/prisma/schema.prisma                                            | 3 ++-
 2 files changed, 5 insertions(+), 1 deletion(-)

## Diff
diff --git a/server/prisma/migrations/20260715120000_add_clerk_identity/migration.sql b/server/prisma/migrations/20260715120000_add_clerk_identity/migration.sql
new file mode 100644
index 0000000..81fde1e
--- /dev/null
+++ b/server/prisma/migrations/20260715120000_add_clerk_identity/migration.sql
@@ -0,0 +1,3 @@
+ALTER TABLE "users" ALTER COLUMN "password_hash" DROP NOT NULL;
+ALTER TABLE "users" ADD COLUMN "clerk_user_id" TEXT;
+CREATE UNIQUE INDEX "users_clerk_user_id_key" ON "users"("clerk_user_id");
diff --git a/server/prisma/schema.prisma b/server/prisma/schema.prisma
index 79fd8ff..db16614 100644
--- a/server/prisma/schema.prisma
+++ b/server/prisma/schema.prisma
@@ -3,21 +3,22 @@ generator client {
 }
 
 datasource db {
   provider = "postgresql"
   url      = env("DATABASE_URL")
 }
 
 model User {
   id           String         @id @default(uuid())
   email        String         @unique
-  passwordHash String         @map("password_hash")
+  passwordHash String?        @map("password_hash")
+  clerkUserId  String?        @unique @map("clerk_user_id")
   createdAt    DateTime       @default(now()) @map("created_at")
   updatedAt    DateTime       @updatedAt @map("updated_at")
   tokenVersion Int            @default(0) @map("token_version")
   jobs         Job[]
   notifications Notification[]
 
   @@map("users")
 }
 
 model Job {
