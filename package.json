{
  "name": "ampm-root",
  "version": "1.0.0",
  "private": true,
  "description": "AMPM - Root workspace configuration",
  "scripts": {
    "prepare": "husky",
    "lint": "npm run lint --prefix client && npm run lint --prefix server && npm run lint --prefix worker",
    "lint:fix": "npm run lint:fix --prefix client && npm run lint:fix --prefix server && npm run lint:fix --prefix worker",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "biome:check": "biome check .",
    "biome:format": "biome format --write .",
    "test": "npm test --prefix client && npm test --prefix server && npm test --prefix worker"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.1",
    "prettier": "^3.4.2",
    "typescript-eslint": "^8.65.0"
  }
}
